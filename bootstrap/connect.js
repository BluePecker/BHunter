/**
 * Created by shuc on 17/8/5.
 */

class Connect {
    // connect mysql
    Mysql(opt) {
    }
    // connect redis
    Redis(opt) {
    }

    // connect mongodb
    Mongodb(opt) {
    }
}

export default new Connect();

{
    "apps": [
        {
            "name": "service",
            "script": "/usr/bin/git pull origin master && ./app.js",
            "watch": true,
            "env": {
                "NODE_ENV": "production"
            }
        }
    ]
}